{{ if and .chezmoi.tags.linux (eq .chezmoi.os "linux") }}#!/usr/bin/env bash
set -euo pipefail

if command -v apt-get >/dev/null 2>&1; then
  sudo apt-get update
  sudo apt-get install -y zsh-syntax-highlighting
fi
{{ end }}